<thead>
  <tr>
    <th class='compare' title='Compare - Select item for compare'>
      c
    </th>

    <th class='hidden'>
    </th>

    <th class='name'>
      Name
    </th>

    <th class='tier hide_on_small'>
      Tier
    </th>

    <% weapon.values.order(:stat_hash).each do |value| %>
      <th class='stat' title='<%= stat_description(value.stat) %>'>
        <%= (value.stat.name || 'N/A')[0..4] %>
      </th>
    <% end %>
  </tr>
</thead>
